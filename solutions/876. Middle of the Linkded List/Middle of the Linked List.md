## 題目說明
- 給予一個單向鏈結串列(singly linked list)，返回該串列的中間list
- 若中間的list有兩個，則回傳第二個

## 解題思路
### 方法一：O(n)
1. 第一次迴圈計算總共有幾個節點
2. 知道節點總數後，第二個迴圈只要走1/2個節點數量，既可以找到中間的節點
  - 若節點數量是奇數，要走的節點數量要使用Math.floor

### 方法二：快慢指針
1. 當慢指針走一步的時候，快指針會走兩步
2. 所以當快指針走到null的時候，慢指針會剛好到中間