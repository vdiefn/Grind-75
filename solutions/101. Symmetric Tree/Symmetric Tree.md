## 題目說明
判斷給予的一棵二元樹是否為鏡像對稱

## 解題思路
1. 左邊節點的左子樹會等於右邊節點的右子樹(內側/外側都對稱)
2. 數值也要一致
3. 使用遞迴，從上往下判斷，若有發現不一致的狀況可直接回傳false提早結束查詢
4. 基本判斷：
    - 若這個root是null的狀況，則可以直接視為對稱
5. 輔助函式中的判斷：
    - 若兩個node都是null -> 對稱
    - 若兩個node中有一個是null -> 不對稱
    - 若兩個node的值不一樣 -> 不對稱