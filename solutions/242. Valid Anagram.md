### 題目說明
如果兩個字串滿足以下兩個條件，它們就是 Anagram：

1. 組成字元完全相同：s 有哪些字母，t 就必須有哪些字母。

1. 字母出現次數完全相同：s 如果有 3 個 'a'，t 也必須剛好有 3 個 'a'。

### 解題思路
1. 計算字串s中出現的字母以及每個字母出現的次數
2. 將計算出來的obj和字串t中的每個字母做比較
  - 當obj中沒有字串t的某個字母，即可return false
  - 當obj中出現了字串t的某個字母，要將obj中該字母的統計次數減一
  - 全部遍歷一遍沒有問題即可return true