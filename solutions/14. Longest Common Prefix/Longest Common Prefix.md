## 題目說明
寫一個函式，用來找出字串陣列中的「最長共同前綴 (Longest Common Prefix)」。如果沒有共同的前綴，則回傳空字串 ""

## 解題思路
1. 如果strs的長度為0可以直接回傳`""`
2. 先取strs[0]這個單字中的字母當作對照的標基準，接著和後面的每一個單字做比較
3. 也就是會需要兩個迴圈，第一個迴圈負責選擇strs中每一個單字，第二個迴圈負責選中的那個單字中的每個字母
4. 一旦發現目前遍歷到的字母和標準字母不一致，就將標準字母切片(0, 目前遍歷到的位置)，並回傳
5. 如果一直都沒有出現不一致的狀況，也就是說標準字母就是最後的答案