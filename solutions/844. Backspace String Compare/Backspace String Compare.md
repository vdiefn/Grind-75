## 題目說明
給定兩個字串 s 和 t，如果這兩個字串在輸入空的文字編輯器後結果相等，則回傳 true。其中字元 '#' 代表 退格鍵（Backspace）。
> - 在空白的編輯器中按下退格鍵後，文字內容依然會維持空白。
> - `#` 會消除它左邊最近的一個有效字元。

## 解題思路
#### 複雜度：時間 $ O(M + N) $ / 空間 $ O(M + N) $
1. 建立一個stack，每遇到一個字母就將字母放進去，每遇到一個 ` # ` 就將stack裡最後放進去的字母拿出來
2. 最終比較兩個stack是否一致
> 重複的程式碼要記得使用輔助函式包起來
